# fly.toml app configuration file generated for vulavula-claude-chat on 2026-06-04T10:41:44+02:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'vulavula-claude-chat'
primary_region = 'jnb'

[build]

[env]
  # Lelapa's own private OmniVoice Space (ZeroGPU). Private, so requests need
  # OMNIVOICE_TOKEN — set as a Fly secret, not here.
  OMNIVOICE_SPACE_URL = "https://lelapa-omnivoice.hf.space"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 512
